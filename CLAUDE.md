# 모바일 청첩장 프로젝트

## 무엇을 만들고 있나
GitHub Pages로 호스팅하는 한 페이지짜리 모바일 청첩장.
공개 주소: https://a01051998767.github.io/welcome

## 기술 스택
- 빌드 도구 없음. `index.html` 한 파일에 HTML + CSS + JS를 모두 담는다.
- 프레임워크를 도입하지 않는다. 이 결정은 의도적이다.
- 배포는 `git push` 하면 GitHub Pages가 자동 반영한다. 별도 빌드 단계 없음.

## 파일 구조
```
index.html      ← 화면에 필요한 것은 전부 여기에 들어있다
images/         ← cover.jpg, photo-1~5.jpg, og-image.jpg, apple-touch-icon.png
apps-script.gs  ← 참석 의사를 시트에 받아 적는 코드. 붙여넣기용이라 따로 둔다
CLAUDE.md       ← 이 파일
```
파일은 되도록 늘리지 않는다. 전통 문양 SVG도 `index.html` 안에 직접 넣어
`patterns/` 폴더를 없앴다. 새 장식이 필요하면 같은 방식으로 안에 넣는다.

## index.html 구조
파일 하단 `const CONFIG = { ... }` 블록에 모든 내용(텍스트·사진·계좌·링크)이 모여 있다.
그 아래 렌더링 코드는 CONFIG를 읽어 화면을 그린다.
**내용을 바꿀 때는 CONFIG만 수정한다.** HTML 본문에 텍스트를 직접 써넣지 않는다.

### 다국어
- 한국어/영어 2개국어. `{ ko:"...", en:"..." }` 형식.
- 두 언어가 같으면 문자열 하나만 써도 된다. `t()` 헬퍼가 처리한다.
- 새 문구를 추가할 때는 반드시 ko/en 둘 다 채운다.

### 섹션 순서
표지 → 인사말 → 두 사람 → 사진 → 예식 순서(1·2·3부) → 안내 사항 → 오시는 길 → 참석 의사 → 마음 전하실 곳 → 고마운 분들 → 공유 → 맺음

### 예식 구성
- 1부 전통혼례 / 2부 피로연 / 3부 맥주 파티
- 2부에는 비건·채식 메뉴 안내와 축가 가사 접이식 패널이 있다.
- 안내 사항 섹션: 접근성 안내(첫 항목), 문자통역 링크, 장애인 화장실·이동 경로, 온라인 중계 링크. 링크가 여럿이면 `links` 배열을 쓴다.
- 오시는 길: 예식장 이름·주소 → 지도 → 지도 앱 링크(카카오지도·네이버지도·구글지도) → 교통 안내 차례다. 교통 안내는 `steps` 를 쓰면 번호 목록으로 나온다.
- 3부 장소는 `program[2].place` 에 있다. 버튼을 누르면 주소·지도·지도 앱 링크가 접이식으로 열린다. 축가 가사와 같은 `.song-toggle` 방식을 쓰며 `aria-controls` 로 짝을 짓는다.
- 고마운 분들 섹션: 도움 주신 분·기관 목록. `url`을 적으면 이름이 홈페이지 링크가 되고, 비우면 글씨로만 나온다.
- 상단 도구 막대: 언어 · 글씨 크기 · 차례(햄버거). 차례는 `render()`의 `menuItems` 목록으로 만들며 섹션 `id`로 건너뛴다. Esc·바깥 클릭으로 닫히고 고른 자리로 초점이 간다.
- 하단 고정 바: "참석 의사 전달하기". 섹션 안 버튼과 같은 입력창을 연다. 바 높이는 `--bar-h`로 재어 `.paper` 아래 여백에 넣는다.
- 위아래 막대 높이는 글씨 크기에 따라 달라지므로 `ResizeObserver`로 재어 `--top-h`(도구 막대) · `--bar-h`(하단 바)에 넣는다. 표지 위 여백, 차례 위치, 건너뛰기 목적지 여백이 이 값을 쓴다. 고정 px로 되돌리지 않는다.
- "본문으로 건너뛰기" 링크는 키보드·스크린리더 이용자가 도구 막대를 건너뛰는 장치다. 지우지 않는다. 평소에는 `transform`으로 제 높이만큼 위로 숨기고 초점을 받으면 내려온다. `top:-60px` 같은 고정값으로 숨기면 글씨를 키웠을 때 화면에 걸친다.
- 공유 섹션: 링크 공유 버튼 아래에 웹접근성 안내(`a11yNote`), 라이선스 안내와 저장소 링크(`repoUrl`)가 붙는다.
- 참석 의사로 받는 항목: 성함(필수) · 1·2·3부 참석 · 미리 알려주실 것(자유롭게) · 연락처. 신랑측/신부측과 비건 체크는 쓰지 않는다. 식사·이동·통역 같은 요청은 "미리 알려주실 것"에 적게 한다.
- 참석 의사: 구글 폼을 쓰지 않는다. 버튼을 누르면 같은 화면에서 `<dialog>` 입력창이 열리고, `CONFIG.rsvpEndpoint`(Apps Script 웹 앱)로 보내 시트에 쌓인다. 주소가 비어 있으면 "준비 중" 안내만 하고 아무것도 보내지 않는다. 성공은 서버가 돌려준 접수 번호로만 판단한다. `no-cors`로 바꾸거나 오류를 무시해 성공 처리하지 않는다.
- 축가 가사에는 작사·작곡·노래(`song.credit`)와 저작권 안내(`song.note`)를 반드시 함께 표시한다.

## 반드시 지킬 것

### 1. "혼주"라는 단어를 쓰지 않는다
부모님은 신랑·신부를 기준으로 표기한다.
- 맞음: `김OO의 아버지 김OO`
- 틀림: `혼주 김OO`

### 2. 웹접근성을 깨뜨리지 않는다
이 사이트는 전맹·저시력·색약 이용자를 고려해 만들었다. 코드를 고칠 때 아래를 유지한다.
- 모든 사진에 의미 있는 `alt` 설명. "사진1" 같은 표현 금지.
- 장식 요소(囍, 도장의 一二三, ♡, 영문 장식 문구)는 `aria-hidden="true"`.
- 본문 대비 7:1 이상 유지. 색상 변수(`--ink`, `--ink-soft`, `--jade`)를 임의로 밝게 바꾸지 않는다.
- 색만으로 정보를 전달하지 않는다. 링크는 밑줄, 상태 변화는 글자도 함께 바뀌어야 한다.
- 글씨 크기는 `px`가 아니라 `rem`으로 쓴다. 상단 글씨 크기 버튼(100/125/150%)이 동작해야 한다.
- 초점 표시(`:focus-visible`)의 주황색(`--focus`)은 적록색약 대비용이다. 초록으로 바꾸지 않는다.
- 클릭·터치 영역은 최소 44px.
- 자체 읽어주기(TTS) 기능은 넣지 않는다. 이용자의 스크린리더와 음성이 겹쳐 방해가 된다.
- 갤러리 사진은 `<button>`이어야 한다. 키보드로 열 수 있어야 한다.
- 사진 확대창은 Esc로 닫히고, 닫으면 원래 위치로 초점이 돌아와야 한다.

### 3. 저작권
축가(다시 만난 세계) 가사는 저작권이 있다. 코드에 가사를 채워 넣지 말고 자리표시자로 둔다.

## 라이선스
코드는 MIT(`LICENSE`). 사진·축가 가사·개인 정보는 제외이며, 그 범위는 `LICENSE` 아래쪽에 적어 두었다.
폰트 Gowun Batang · Noto Serif KR · Noto Sans KR는 모두 SIL OFL 1.1이라 상업적 사용에 문제가 없다.
전통 문양 SVG와 공유 카드(`images/og-image.jpg`)는 이재 님이 만들어 주셨다. MIT 범위에서 빼두었고, 다른 곳에 쓰려면 따로 여쭤봐야 한다.

## 디자인 방향
- 전통혼례에 맞춘 초록·크림 계열. 크림 바탕(`--paper` #F7F4EC)과 연녹 바탕(`--paper-warm` #E7EBE1)이 섹션마다 번갈아 나온다.
- 카드·패널은 `--card`를 쓴다. 섹션 바탕이 크림이면 카드가 연녹, 연녹이면 카드가 크림으로 자동으로 뒤집힌다.
- 소나무색(`--pine` #2E5B4F)은 버튼·맺음 바탕, 청자색(`--jade` #275044)은 강조 글씨.
- 금색(`--gold`, `--gold-soft`)은 **장식 전용**이다. 대비가 낮으므로 글씨에 쓰지 않는다.
- 붉은 낙관 도장색(`--seal`)은 1·2·3부 표시에만 아주 좁게 쓴다.
- 섹션 머리는 `문양 → 한글 제목 → 영문 레터스페이싱 라벨` 순서다. 문양은 `index.html` 위쪽 `<svg class="sprite">` 안에 `<symbol>`로 들어있고 `<use href="#orn-…">`로 불러 쓴다. 장식이므로 `aria-hidden`, 색은 CSS `color`로 정한다.
- 서체: 제목·본문 Gowun Batang(명조), 보조 Noto Serif KR, 라벨·숫자 Noto Sans KR.
- 모바일이 기본. 최대 폭 480px 한 칸으로 고정하고 데스크톱에서는 가운데 정렬.

## 아직 안 채운 것
- [x] 예식 날짜·시간·장소 — 2026. 10. 24.(토) 17시 · 관악문화예절원 (공유 미리보기 메타 태그에도 같은 값이 들어 있으니 바뀌면 둘 다 고친다)
- [x] 사진 — cover.jpg 1장, photo-1~3.jpg 3장. 규격에 맞게 잘라 넣었다
- [x] 각 사진의 alt 설명
- [x] 참석 의사 접수 주소 (`rsvpEndpoint`) — 연결함. 실제 제출 테스트는 배포 후 한 번 확인 필요
- [x] 지도 embed (`mapEmbed`) — 키 없이 쓰는 구글 지도 주소. 네이버 지도는 iframe 삽입을 막아 두어 키 없이는 화면에 넣을 수 없다.
- [x] 더 베어 낙성대점 주소 — 서울 관악구 낙성대로 20
- [ ] 문자통역 링크, 온라인 중계 링크
- [x] 장애인이 이용할 수 있는 개방화장실 두 곳 — 관악문화예절원 · 낙성대역. 안내 사항에 카카오지도 링크 버튼으로 넣었다
- [ ] 비건 메뉴 이름
- [ ] 축가 가사
- [ ] 계좌번호
- [x] og-image.jpg (카톡 공유 미리보기, 1200×630) — 이재 님 제작
- [ ] 고마운 분들 명단 (`credits`) — 이재 님은 넣어두었다. 나머지 분·기관 이름, 한 일, 홈페이지 주소

## 이미지 규격
- cover.jpg — 3:4 세로, 900×1200px, 300KB 이하
- photo-1~5.jpg — 4:5 세로, 1000×1250px, 각 250KB 이하 (지금은 photo-1~3 세 장)
- og-image.jpg — 1200×630px
- apple-touch-icon.png — 180×180px. 홈 화면에 추가했을 때 쓰는 그림. 모서리는 iOS가 알아서 둥글게 자르므로 네모로 만든다. 탭 아이콘은 파일 없이 `<link rel="icon">` 안에 SVG로 넣어 두었다

## 작업 방식
- 커밋 메시지는 한국어로 짧게 쓴다.
- 큰 변경 전에 무엇을 바꿀지 먼저 설명하고 확인을 받는다.
- 파일이 하나뿐이므로 전체를 다시 쓰지 말고 필요한 부분만 수정한다.
